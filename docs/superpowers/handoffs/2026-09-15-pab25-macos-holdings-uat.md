# PAB-25 macOS 同花顺持仓读取 UAT 记录

## 结论

**Inconclusive（不确定）**。实现与聚焦自动化检查已完成，但所有发布阻断级真实产品检查均为 `not-tested`。在签名 Electron 宿主、已登录且具有安全测试持仓的同花顺环境中完成下表前，不得把 macOS 客户端兼容性标记为已验收或建议发布。

## 环境

- 日期：2026-09-15
- 开发环境：macOS arm64，独立 worktree `codex/macos-holdings-accessibility`
- 同花顺：`/Applications/同花顺.app` 已安装；检查时未运行
- Electron：没有本轮签名验收包
- 服务：worktree-writer 未获分配独立端口，未启动或操作共享 3080 服务
- 账户与数据：没有确认可安全替换的真实/模拟测试账户及持仓
- 视口、主题、截图或录屏：未产生真实产品证据

## 深度验收矩阵

| 关注点 | 风险或原因 | 状态 | 可观察证据 | 剩余风险与下一步 |
|---|---|---|---|---|
| 真实入口 | 必须从持仓明细进入同步面板，并经过签名宿主和真实 TCC 权限 | not-tested | 仅确认同花顺应用已安装；没有运行签名 Electron 包 | 用签名包从真实入口进入，记录辅助功能与自动化权限归属 |
| 关键链路 | 读取、成交明细、兜底时间修改和确认替换会改变本地持仓 | not-tested | 自动化契约测试通过，但没有真实券商表格或业务状态 | 使用安全模拟账户完成读取 → 预览 → 修改兜底 → 原生确认 → 重进页面核对 |
| 失败与恢复 | 同花顺弹窗、表格变化、取消或超时可能留下前台、子进程或错误状态 | not-tested | 单元测试覆盖协作式取消和焦点恢复调用 | 在 AX 读取和 AppleScript 降级期间分别取消，核对无写入、无遗留任务且焦点返回 |
| 持久化 | 长期授权与手动时间必须跨重启保存并可关闭 | not-tested | 存储单测与设置组件测试通过 | 重启签名应用，核对长期授权不重复询问、仍只响应主动点击；关闭后再次读取应重新询问 |
| 真实/模拟隔离 | 错误账户会造成持仓归属错误 | not-tested | 契约测试区分 `broker_real` 与 `broker_simulated` | 分别登录或进入两类账户，核对路径、账户标签、持仓和快照来源 |
| 响应式与主题 | 预览新增第四列、展开明细和时间输入，可能溢出或遮挡动作 | not-tested | jsdom 组件测试不构成真实渲染证据 | 检查浅色/深色、1440px、1024px、应用最小 900px、长中文和大数值；保存截图 |
| Windows 回归 | Windows 使用独立 easytrader 实现，macOS 环境不能证明它 | not-tested | 本轮没有修改 `easytrader.py` | 在 Windows 专用任务或 CI 中执行其既有契约与真实客户端回归；不得写成“仅支持 macOS” |

## 支持性自动化证据

- 后端读取、成交明细、取消与时间来源：57 项测试与 2 个 subtest 通过。
- 后端私有/通用接口：14 项测试通过。
- 持仓快照：受影响的 `PortfolioSnapshotTests` 7 项通过。
- Electron 主进程：8 项测试通过；`holdings-native.ts` 独立严格类型检查通过。
- 投研 Runtime：13 项测试通过；Runtime 源文件独立严格类型检查通过。
- 工作台：52 项组件测试通过；设置长期授权：1 项组件测试通过。
- Python 变更文件 `py_compile` 与 `git diff --check` 通过。
- Agent Note 格式 575 项通过；四组已修改中英配对文档一致性检查通过。

## 受限门禁

- 主题检查在本分支基线的既有字面颜色和内联样式处失败；本次新增 CSS 行未出现在违规清单中，但整体门禁仍不是通过状态。
- API catalog 校验在无关的 E2B `unknown` 类型分析处失败；本次 `nativeHoldings` 生成条目已与源码和双语子系统文档同步。
- 目标包完整 TypeScript build 受当前 worktree 不完整依赖链接与缺失前置 `lib/types` 产物限制；改变文件使用聚焦测试，Electron 与 Runtime 源文件另做独立严格类型检查。

## 关闭条件

由 `local-integrator` 或获分配独立端口的验收任务构建签名 Electron 包，在已登录同花顺的安全模拟账户上完成矩阵中除 Windows 外的所有 `not-tested` 项并保存截图或录屏。Windows 回归应由 Windows 环境单独记录。任一发布阻断项失败时，PAB-25 保持未完成并修复后重验。

# PAB-17 Web 单用户管理员鉴权实施计划

1. 在 `packages/api/web-auth` 建立独立 Host 服务，完成密码哈希、配置校验、会话、CSRF、限流、健康检查及连接撤销，并用聚焦单测锁定安全边界。
2. 在 Webserver 导出请求信任工具，连接层在 HTTP、通用 RPC 与 WebSocket 的业务处理前统一调用鉴权，同时保留现有回环特权限制。
3. 在 `apps/web` 实现会话门禁、错误状态矩阵与退出入口；客户端传输自动附带 CSRF，并把 401 提升为全局失效事件。
4. 在 Web 组合和启动参数中接入环境配置，只在鉴权闭合时允许全接口绑定；CLI 增加不经 argv 传递明文的密码哈希命令。
5. 补充中英文运维文档和 Agent Note，更新依赖锁文件，完成聚焦测试、样式检查、构建及 3091 真实浏览器验收。
6. 自审 diff 后提交、推送到私仓并创建跨仓 PR；把验证证据和剩余边界写回 PAB-17，状态改为 In Review。

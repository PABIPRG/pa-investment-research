# PAB-11 Deep UAT 记录

**Ready** — PAB-11 的发布阻断项均已在隔离真实产品环境通过；自动化门禁同时通过。

## 验收范围

- Linear：PAB-11 `[P1][EVO-001/002] 补齐自进化五维状态语义与首屏运行指标`
- RC10：`RC10-E2E-21`、`RC10-E2E-22`
- 分支：`codex/rc10`
- 隔离环境：前端 `127.0.0.1:3180`、后端 `127.0.0.1:8180`
- 隔离状态目录：`/private/tmp/pa-rc10-pab11-uat`

## 自动化支持证据

自动测试与构建只作为支持证据，不代替真实产品 UAT。

| 命令 | 退出码 | 真实结果 |
|---|---:|---|
| `pnpm exec vitest run packages/client/ui-investment-research/tests/evolution-entry.client.spec.tsx packages/client/ui-investment-research/tests/assistant-intent.client.spec.ts --reporter=dot` | 0 | 2 个测试文件、21 个用例通过 |
| `pnpm run typecheck:contracts-ready` | 0 | `tsc -b tsconfig.client.json` 通过 |
| `pnpm --filter @deepseek-ai/dsh-client-ui-investment-research run bundle` | 0 | ESM 与 Client CJS 构建完成；只有既有平台与弃用警告 |

## UAT 矩阵

| 关注项 | 风险/原因 | 状态 | 可观察证据 | 残余风险/下一步 |
|---|---|---|---|---|
| 真实入口 | 导航不可达会使功能无法使用 | passed | 从真实「研究工作台」点击「自进化」，进入标题为「自进化 · 闭环看板」的页面 | — |
| 首屏权威状态 | 前端可能把生命周期数量误作演化结果 | passed | 后端 `evolution_counts` 为 normal/watch/promote/retire=`1/1/1/1`，页面对应显示 `1/1/1/1`；同页生命周期 active=`3`，未被误算为 normal；最近动作显示 5 项 | — |
| 运行时间与计划 | 最近运行与最近应用可能混用，计划时间可能模糊 | passed | 首轮状态显示最近自动运行 `2026-09-04 00:25:53 UTC+08:00`、上次自动应用 `2026-09-04 00:25:36（服务本地时间）`；关闭闭环时下次计划明确显示「自动闭环未启用」；重建后的空运行历史明确显示「尚无运行记录」 | — |
| 策略五维事实 | 参与、验证、置信、来源、任务可能混用 | passed | 「演示·升级与变异」列表与详情均分别显示正常运行、已验证通过、已升级、演示数据和任务状态；候选变体详情显示「参与状态：候选」「来源：变异来源」及母策略 `demo-promote` | — |
| AI 权威上下文 | 模型可能自行推断产品事实 | passed | 从详情点击「AI 解释当前判定」，真实输入框预填同一五维状态，并明确「这些字段是产品事实，不由模型推断」 | 未发送真实模型请求；PAB-11 只验收上下文交接，不验收模型供应商 |
| 报告语义 | 报告可能继续使用旧标签 | passed | 通过真实「投研报告」入口打开 `demo-promote` 报告，正文分别显示参与状态正常运行、验证分类已验证通过、置信层级已升级、来源演示数据、任务状态已完成 | 隔离 fake runner 无行情，报告由同一生产 renderer 与 ReportStore 写入后通过真实 API/UI 读取；任务成功链由自动化测试支持 |
| 持久一致 | 刷新/重访可能丢失或改变权威数据 | passed | 页面 reload 后从真实导航重访，5 日完成度、四桶 `1/1/1/1`、生命周期和最新动作与同一后端状态一致 | 页面路由本身刷新后回到工作台，按产品当前导航模型重新进入 |
| 失败与恢复 | 服务失败时可能暴露内部错误或无法恢复 | passed | 主动停止隔离 8180 后点击刷新，页面仅显示「投研服务暂时不可用，请稍后重试。」且刷新仍可操作；重启后端再次刷新，错误消失并恢复权威状态 | — |
| 桌面浅色 | 宽屏布局和浅色对比度可能异常 | passed | 1440×900 浅色模式下标题、状态卡、策略卡与操作可见；`body/html scrollWidth=1440`，无横向溢出 | — |
| 移动深色 | 窄屏可能溢出或控件不可操作 | passed | 390×844 深色模式下 `body/html scrollWidth=390`；刷新、AI 解释均 enabled，候选分组点击后 `aria-expanded=true` | — |

## RC10 映射

| 用例 | 状态 | 证据 |
|---|---|---|
| `RC10-E2E-21` | passed | 正常、观察、升级、淘汰结果文案一致；变异只作为来源并在候选详情显示「变异来源」；列表、详情、报告、AI 五维文案合同一致 |
| `RC10-E2E-22` | passed | 首屏显示闭环状态、5/5 日数据门槛、四类结果计数、生命周期计数、最近动作和策略衍生关系入口 |

## 关键原始证据

- 首屏状态卡：正常/观察/升级/淘汰 `1/1/1/1`，生命周期正常运行 `3`，证明结果桶未由生命周期推导。
- 策略详情：`参与状态：正常运行；验证结果：已验证通过；置信等级：已升级；来源：演示数据；任务状态：暂无任务`。
- AI 输入框：包含相同五维事实及「这些字段是产品事实，不由模型推断」。
- 报告正文：包含 `参与状态：正常运行`、`验证分类：已验证通过`、`置信层级：已升级`、`来源：演示数据`、`任务状态：已完成`。
- 浏览器控制台错误：0 条。
- 本轮未保存或提交二进制截图；视觉布局由真实浏览器尺寸、可见 DOM 与滚动宽度共同验证。

## 数据说明

- 初次报告检查命中了隔离目录中上一轮生成的旧报告，仍含旧文案「参与状态：生效」。这不是当前代码生成结果。
- 停止隔离后端后，以 `scripts/prepare-demo-evolution-data.sh prepare` 重建隔离演示状态，再用当前生产 `render_strategy_report` 与 `ReportStore` 写入报告；真实报告 API 和 UI 均显示新合同「参与状态：正常运行」。
- 为生成报告尝试的 fake 回测因隔离环境无行情而失败，页面如实显示其最新任务状态为失败；该失败不影响五维字段彼此独立的验收结论。

## 环境清理

- 本轮只使用 3180/8180，未操作共享 3080。
- 本轮启动的前后端均已停止；连接检查确认 3180/8180 均无法连接。

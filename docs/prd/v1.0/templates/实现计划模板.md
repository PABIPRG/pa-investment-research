# 实现计划模板

> 状态：草案
> 对应设计文档：
> 需求编号：
> 目标分支：`codex/<topic>`

## 1. 完成定义

列出用户可观察结果、自动检查和产品验收证据。

## 2. 基线检查

- 当前分支：
- `public/master` 提交：
- 工作区现有改动：
- 远端映射：
- 依赖与运行环境：

## 3. 实现切片

| 顺序 | 垂直切片 | 需求/设计引用 | 影响模块 | 验证 |
|---|---|---|---|---|
| 1 |  |  |  |  |

每个切片应能独立验证，避免把前端、后端和测试拆成长期互相不可用的孤岛。

## 4. 并行与依赖

说明可并行任务、共享合同、文件冲突和合并顺序。共享接口先冻结，再分派页面实现。

## 5. 测试计划

- 失败复现：
- 单元与组件：
- 合同与后端：
- 浏览器关键旅程：
- 视觉与可访问性：
- 全量回归：

## 6. 数据与迁移

说明新旧状态、一次性迁移、幂等、备份和回滚。

## 7. 风险控制

列出高影响写入、敏感数据、并发、超时、失败恢复和 feature flag。

## 8. 提交与 PR

- Codex commit message 使用 `[AI] ` 前缀。
- 只在用户明确要求时提交、推送或创建 PR。
- 特性分支只推送到 `private`，通过跨仓 PR 进入 `public/master`。

## 9. 交付证据

- 测试命令与结果：
- 截图或浏览器记录：
- 追踪矩阵更新：
- 已知限制：
- 回滚验证：

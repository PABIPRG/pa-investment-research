# PAB-28 手工成交录入

## 当前 PR 交付状态（2026-09-18）

用户已授权提交、推送并创建 PR，当前交付分支为 `codex/notification-business-copy`；下文保留各阶段实施与验收记录。本 PR 提供手工成交录入、预览/保存及持仓界面改进，**历史成交修改尚未实现**，PAB-28 保持 In Progress。真实页面已覆盖交互、搜索、预览、父子弹窗、响应式和浅深色；真实成交保存、刷新持久化与有记录历史验收仍未完成。完整 GUI 存在未改模块失败，Web 回放未完成，均不标记通过。本次采用草稿 PR 收集审查，剩余能力和验证债务由当前实现者及后续集成者在标记可合并前完成；不因 PR 创建标记 Done。

用户确认：仅记录已发生成交，更新研究持仓，不执行券商订单。沿用当前专属 worktree、基准 38179444、目标 codex/notification-business-copy。

## 实施与一致性

1. 复用持仓快照规范，在同一 holdings 文档原子保存手工成交与更新后持仓。避免持仓成功而成交历史失败。券商成交仍由既有 trades 集合管理，历史入口只组合读取。
2. 提供预览/确认接口，以持仓版本和请求编号防止过期保存与重复入账；使用 Decimal 计算成本。逐标的拒绝早于最近持仓变更的成交。
3. 当前持仓行提供买入、卖出、历史，工具栏提供新标的买入及全部历史。使用 UI 库 Button/Input，保留隐私遮罩。先展示变更预览再保存。
4. 保存后刷新研究工作台及风险计划；清仓不删除成交历史。已有同步、导入和编辑继续保留文档其他字段。

## 体验状态

加载、错误、空历史、禁用、成功、确认适用；预览失败保留输入，保存失败允许同编号重试。过期数据拒绝保存并重新预览。无独立权限模型；不支持部分成功。筛选按当前标的，空结果使用空态。窄屏采用换行操作及横向历史表滚动。

## 验证

先验证买卖、费用、清仓、超卖、时间、幂等和并发版本；再验证运行时映射、界面表单及类型。真实 UAT 需要隔离端口分配；不得操作共享 3080。未验证项目明确记录，不标记 Done。

## 当前验证与剩余工作（2026-09-17）

- 后端隔离依赖 `/private/tmp/pab28-test-deps`：`tests.test_manual_trades`、`tests.test_trades_store`、`tests.test_portfolio_performance.PortfolioSnapshotTests` 共 25 项通过，含买卖、清仓、同步后的时间边界、失败原子性、幂等与并发旧版本。
- 前端成交面板、持仓既有交互、Python runtime 映射共 70 项通过，日志 `/private/tmp/pab28-final-focused.log`。类型检查与包构建通过。
- 新增面板和修改的组件无主题规则违规；InvestmentShell.module.css 仍为基线已有 4 项，无新增（此比较未验证新增 token 定义，新增代码没有新 token）。
- 扩展行情测试有 4 项因隔离环境缺少 akshare 未运行；未改动行情逻辑。包括工作台完整 spec 的联合运行未正常结束，已停止本次测试进程，不能标记其通过。
- 未运行本轮全量 GUI/Web，不复用上轮失败为通过；真实 UAT 未完成。3080 属于共享 local-integrator 服务，当前角色不得管理；隔离服务端口仍未获分配。验证债务负责人为当前实现者与后续 local-integrator，必须在合并/发布前关闭。
- 用户追加：历史操作记录允许修改。已提出保存修改前后审计及预览影响；待用户选择“连续手工成交重算，遇券商同步/导入/直接编辑则仅改历史”或“始终只改历史”。尚未实施修改接口和入口，不能宣称 PAB-28 完成。
- 原始持仓快照不因历史录入回写旧时间；成交保留实际时间，快照记录本次保存生效时间，避免其他标的较新快照被倒序插入。
- 尚未提交、推送或合并，Linear PAB-28 保持 In Progress。

## 持仓与成交界面整改及真实验收（2026-09-17 晚）

用户在后续对话中确认切换为 local-integrator，并停止旧 3080 服务、授权从当前 worktree 重启。已初始化工作树独立 Python 运行环境并完成前后端构建；当前 3080 提供本工作树内容。此记录替代上文“无权管理 3080、真实 UAT 未开始”的环境限制，未替代完整成交链路验证债务。

- 对应用户六条界面反馈：按钮统一为 8px 圆角长方形；工具栏按可用宽度排列；行操作固定为三列网格；风险解释使用现有 UI 库的图标及 Tooltip，默认隐藏说明，支持鼠标悬停和键盘聚焦。
- 成交录入采用独立表单布局：第一行标的和较宽的成交时间，第二行数量、价格及费用；预览按钮放在独立操作区，窄屏转换为单列。保留原有预览、幂等、错误及保存逻辑。
- 成交历史重新组织标题、来源说明、空态图标及“记录一笔买入”入口；有记录时使用独立的可横向滚动表格，避免复用持仓移动卡片样式后丢失字段标题。
- 自动化：成交面板和持仓页面共 27 项通过，日志 `/private/tmp/pab28-ui-polish-tests.log`；UI 包类型检查及 bundle 通过，最终构建日志 `/private/tmp/pab28-ui-polish-build.log`。最后窄屏标签调整仅为 CSS，已重新构建并实机复核。
- 主题检查：使用主题包实际声明验证语义 token；新增面板及修改的组件无违规，Shell 仍仅原有 4 项颜色违规，无新增。未将存量违规视为通过。
- 真实页面证据：从工作台“管理持仓”入口，在 390、768、872、1024、1440 像素宽度检查持仓列表和录入表单；无页面横向溢出、日期输入无重叠，按钮圆角及行列几何正确。检查空历史及空态买入入口、返回持仓、空输入预览报错、风险图标提示和键盘焦点；已检查浅色和深色，并恢复浅色及默认视口。截图在本次浏览器验收对话中。
- 边界：本次真实页面验收没有写入用户持仓；真实有数据成交历史、预览确认后的持仓更新和刷新持久化仍未完成实际写入验收，自动化通过不能替代该证据。本轮未重复全量 GUI/Web。历史修改接口和入口仍未实施，PAB-28 保持 In Progress。

## 子弹窗整改（2026-09-17 22:03）

用户要求解决返回与关闭混淆，并明确选择独立子弹窗。体验契约：持仓明细持续保留；买入、卖出、成交历史、同步及导入打开各自命名的子框。底部红色关闭返回父持仓，不再放置重复的顶部返回按钮；右上角关闭及 Esc 同样只关闭最上层。关闭还原入口焦点，保存中锁定关闭。

能力审计：原 HoldingsEditor 用条件分支替换父弹窗正文，标题与关闭作用域不一致；原 DetailDialog 每实例监听 document.keydown，父子框可能一起响应 Esc。现有 ui-primitives Modal 已提供 Radix 层叠、焦点、遮罩和键盘能力，因此以 Modal headless 承载现有 DetailDialog 外观，删除自制键盘/portal逻辑；HoldingsActionDialog 仅组合业务标题与底部操作。同步转导入、空历史转买入保留子框外壳，防止焦点还原目标被卸载。同步完成返回父框，手动退路进入导入子框。

状态矩阵：加载/空数据/错误/无权限/禁用/成功/确认适用，沿用已有正文和保存状态；本次仅调整层级与关闭路由。刷新及过期沿用已有请求语义；筛选无结果与独立撤销不适用（没有新增筛选或持久操作）；部分成功沿用同步明细说明。导入关闭保留草稿，成交保存失败保留输入；保存成功的父持仓更新契约不变。

验证：新增 9 项子框回归先失败再通过，含所有入口、两层 Esc、初始通知直达同步、两条子流程切换后的焦点还原；工作台 56 项通过，含保存禁关、失败草稿、导入成功及同步完成返回。另成交面板 4、详情 5、数据页 23 项通过，共 97 项。类型及 bundle 通过；主题 token 检查无新增违规（Shell 既有 4 项仍在）。日志 `/private/tmp/pab28-child-final-tests.log`、`/private/tmp/pab28-child-types.log`、`/private/tmp/pab28-child-build.log`。已完成独立只读审查，修复其发现的同步完成路由和减少动画优先级问题。

真实 UAT（Focused）：3080 从管理持仓进入；买入 Esc、卖出底部关闭、导入右上角关闭均保留父框并返回原入口焦点；历史空态转买入可操作；同步 Tab/Shift+Tab 在子框循环。390px 长同步内容与批量导入底部关闭可达，768/872/1024/1440px 无页面横向溢出，检查浅深色并恢复用户浅色和默认视口。上述入口、关闭链路、键盘、响应式为 passed，截图保存在本次对话。遮罩点击实机路径 not-tested；真实成交持久化及历史修改仍为上一节的未完成范围，本次未写入用户持仓，也未重跑全量 GUI/Web。

## 原行编辑、紧凑操作与证券名称搜索（2026-09-17 22:35）

按用户最新九条批注调整：数量与成本直接替换为本行 UI 库 Input，取消不保存、失败保留输入，固定列宽消除切换跳动；焦点进入数量框，保存或取消后回到本行编辑。操作使用 UI 库 Button ghost，统一为“买入／卖出／历史／编辑／删除”，取消固定三列布局。风险状态、Tooltip 图标和文字设置按钮按内容紧凑排列。此设计替代前文的三列行操作网格，子弹窗契约不变。

能力审计：复用现有 holdings-save 与 HoldingEditorDraft，未新增持仓保存路径；现有证券搜索入口分别耦合全局导航与会话上下文，UI 库没有搜索型 Select/Combobox。因此抽取原全局搜索的数据函数 searchSecurities，复用 market-watch.security-search；新 SecuritySearchField 仅组合库 Input、搜索图标和 Button，结果在表单内占位，无自制浮层或新焦点层。名称/代码均可检索，选择后使用真实六位代码；行内买卖锁定并展示名称与代码。重新输入清空已选标的、预览和请求标识，过期异步结果忽略。方向键滚动活动结果，Enter 只选择、不提交表单；Esc 沿用所在子弹窗关闭语义。

状态矩阵：搜索加载、无匹配、错误重试、过期响应忽略、禁用、已选择，以及编辑取消、保存失败/重试/成功适用；持仓刷新沿用已有快照合并逻辑。没有新增权限或部分成功契约。搜索变更失效预览是确认保护，不改动成交成本规则。

自动化：原行编辑与标签回归先失败后通过；持仓子框 11、工作台 56、成交与搜索 7、数据页 23、产品页 33 项，共 130 项聚焦回归通过。覆盖取消、失败保留、重试、名称选中提交代码、修改查询阻止旧证券提交、旧响应隔离、错误重试、空结果、中文输入法、子框关闭与焦点。日志 `/private/tmp/pab28-inline-tests.log`、`/private/tmp/pab28-search-final-tests.log`、`/private/tmp/pab28-inline-final-tests.log`；类型及 bundle 通过（`/private/tmp/pab28-inline-types.log`、`/private/tmp/pab28-inline-build.log`）。独立只读审查发现的焦点、键盘活动结果滚动、历史转买入名称丢失均已修复。主题检查无新增违规；Shell CSS 仍有既有 4 项，Shell TSX 的既有 inline-style 项已与 HEAD 对比确认未新增。git diff --check 通过。

真实 UAT：3080 已加载本工作树最新包，从管理持仓进入。1144px 编辑前后各列 x/width 完全一致，改数量后取消回本行编辑且无保存；新买入输入“科士达”实际匹配 002518，键盘选中后成功请求成交预览，改搜“银行”立即撤销旧预览。390px 八条搜索结果可局部滚动，方向键到第八项仍可见，Esc 仅关子框；持仓操作和底部关闭可达。768px 表格 676px 与容器同宽，无多余横向滚动；行买入显示“科士达 002518”。1024px 表单代码搜索“600519”匹配贵州茅台；1440px 列表与深色表单可读。浅深色截图在本次浏览器验收对话，已恢复浅色和默认视口，保持持仓明细打开供用户验证。

范围限制：没有写入用户真实持仓；实际保存持久化、有记录历史及历史修改仍沿用前文未完成范围，本轮未运行全量 GUI/Web。PAB-28 保持 In Progress，未提交、推送或创建 PR。

## 风险表单字号与搜索浮层（2026-09-17 23:41）

用户最新三条反馈：全局和单股风险正文过大，证券搜索结果撑高表单。本轮契约为两种风险表单统一使用正文 12px、分组标题 13px、辅助提示 11px（实现为 rem）；搜索展开与收起不移动日期、数量、价格、费用和底部操作，维持已选代码及预览失效规则。此节替代上一节“结果在表单内占位、Esc 直接关闭子弹窗”的设计。

能力审计：UI 库 Menu 已拥有锚定定位、外部点击与关闭行为，因此增加可选 content/listClassName 槽位，复用浮层容器并让搜索提供 listbox/status 内容；未增加第三方库或另建定位体系。Modal 将 Radix 的 onEscapeKeyDown 透传给拥有搜索状态的成交子窗，避免捕获阶段 Esc 先关闭子窗。浮层保留在子窗 DOM 内；Tab 在浏览器计算下一焦点前收起，消除 Chromium 默认把可滚动区域当作焦点停靠点的情况。默认 Menu 的菜单语义保持不变，null 槽位仍使用标准菜单。

状态矩阵：加载、无匹配、错误重试、过期响应忽略、禁用、已选择、清除旧选择及预览、Esc/Tab/点外收起适用并沿用原搜索数据路径；持仓刷新、权限和部分成功没有新增规则；本次确认与撤销仅指表单预览和未保存输入，不产生持久变更。

自动化：成交搜索 8、持仓子窗 11、风险所在数据页 23、UI 库 Menu/Modal 等原子组件 28，共 70 项聚焦回归通过。最后 Tab 修正后重新验证成交搜索 8 项。类型检查、投研包 bundle、Web 入口构建通过；后者必需，因为共享 UI 库由入口提供，单独重建业务包不能更新 Menu/Modal。日志为 `/private/tmp/pab28-floating-final-tests.log`、`/private/tmp/pab28-floating-tab-tests.log`、`/private/tmp/pab28-floating-tests.log`、`/private/tmp/pab28-floating-types.log`、`/private/tmp/pab28-floating-build.log`、`/private/tmp/pab28-floating-shell-build.log`。独立只读审查发现的 null 槽位语义和 aria-controls 悬空引用均已修复。主题扫描修改文件无新增违规；全包扫描仍有既有 Shell CSS 4 项及其他 TSX 的存量 inline-style 项，详见 `/private/tmp/pab28-floating-theme.log`。

真实 UAT（Focused，3080 当前 worktree）：791px 下七条代码搜索结果展开前后，弹窗高度均为 444.242px、五个输入框和底部操作位置完全一致。390px 下八条名称结果展开前后弹窗高度均为 733.438px，字段 y 坐标完全一致；方向键滚动到末项可见，Enter 选中后收起。真实点击选项、点击外部、Tab 到成交时间、第一次 Esc 收起搜索、第二次 Esc 关闭子窗保留父窗均通过。全局和单股继承/单独设置的实际计算字号为正文约 12px、标题约 13px、辅助提示约 11px；390px 风险表单无横向溢出，底部取消和确认可达。检查 768/791/1024/1440px、浅深色和长中文；截图保留在本次浏览器验收对话。恢复浅色与默认视口，停在持仓明细。

本轮没有保存用户持仓、成交或风险配置，没有重跑全量 GUI/Web。真实成交保存持久化及历史修改仍为前文未完成范围；PAB-28 保持 In Progress，不以本轮 UI 验收标记完整功能 Done。未提交、推送或创建 PR。

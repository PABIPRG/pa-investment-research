# Agent Note：免登录公开观察室与只读账户投影

Status: implemented

关联 [PAB-30](https://linear.app/pabiprg/issue/PAB-30/建立免登录公开观察室与只读投研投影)。生产目标域名为 `pair-observe.xiexin.dev`；本次没有创建 Vercel Project、写入生产环境变量、绑定域名或部署。

## 问题

离线交互稿定义了一个免登录、只读的账户表现入口，但仓库原有能力只有受保护的 Web API、持仓与成交事实，以及不包含现金的持仓历史表现。直接公开既有接口会放宽认证边界；直接复用持仓表现会把不完整数据误述为全账户权益；把页面放进官网目录又会把两个独立 Vercel Project 的构建与域名生命周期耦合。

## 决策

- 在 `frontend/apps/public-observatory` 新建独立 Vite/React 应用，复用主题、Button、Modal 与应用图标；DatePicker、MonthPicker 下沉到共享 UI primitives。页面不包含演示数据，构建时只接受 `VITE_PUBLIC_API_BASE_URL`。
- 在 trading-core 的账户所有者内增加不可变账户快照和公开 DTO。金额使用定点十进制字符串，`snapshot_id` 与 `data_revision` 贯穿概览、持仓、权益曲线、日历和活动；指定日期缺失时不回退、不插值，公开读取不触发迁移、同步或写入。
- 新建 Host 公开读取包，只接收固定前缀下的 GET、HEAD 与精确 OPTIONS。Origin、Host、查询参数、响应体大小和速率独立校验；后端路径由代码固定，浏览器不能选择内部地址。原有受保护接口判断保持不变。
- 页面按交互稿提供逐日切换、日期与月份选择、留存快照播放、权益轨迹键盘点选、持仓明细、活动筛选与展开详情。历史日期暂停自动刷新；失败刷新保留上次成功快照并给出可恢复提示。
- 生产 Origin 只通过 `DSH_PUBLIC_OBSERVATORY_ORIGIN` 显式注入；未配置时公开 Host 插件不启用。Vercel 预览域名不会自动获得生产数据权限。

## 与交互稿的有意差异

- 原稿顶部的“查看状态”和“市场状态”是离线演示开关，正式页面不保留。
- 原稿使用浏览器原生 date/month 输入；正式页面改为共享自定义选择器，以满足跨浏览器主题、键盘和焦点契约。
- 原稿能切换模拟交易中/休市；当前公开 DTO 没有权威市场状态，正式页面只标识“A 股 · 公开快照”，不推断休市日。
- 正式页面增加 snapshot/revision、新鲜度、部分成功和失败保留提示；这些是原稿未能表达的真实数据边界。

## 真实产品验收

验收环境为 production build、隔离静态端口 3198 与只读模拟 API 端口 3199；截图通过 Codex 浏览器会话逐视口内联检查，没有写入仓库产物。

| 关注项 | 状态 | 可观察证据 |
|---|---|---|
| 真实入口 | passed | 直接打开构建产物后，无登录跳转即可看到“免登录 · 只读”、账户概览、曲线、持仓、活动和日历。 |
| 关键链路 | passed | 日期选择切到 2026-09-18 后全局日期同步；播放从第一条留存快照开始推进，历史阶段禁用刷新并可暂停；活动类型筛选只保留研究记录，无匹配组合显示独立空态；持仓、计算口径和运行过程均可展开，活动详情关闭后返回父弹层。 |
| 键盘 | passed | 权益曲线获得焦点后按左方向键，读屏文本从 2026-09-19 更新到 2026-09-16。 |
| 失败与恢复 | passed | 停止 API 后刷新，旧快照仍保留并显示更新失败提示；恢复 API 再刷新后提示消失且状态回到“公开快照可用”。 |
| 主题 | passed | 深色与浅色均完成真实渲染检查；验收中修复了布尔主题属性误用导致浅色无法生效的问题。 |
| 响应式 | passed | 1440、1024、768、390px 均无页面级横向溢出；390px 下时间控件换行、概览单列、活动和日历仍可操作。 |
| 控制台 | passed | 修复筛选事件生命周期问题后，以干净浏览器会话完成主题、筛选和弹层交互，warning/error 日志为空。 |

## 限制与后续

- 本地验收使用的是契约一致的只读模拟响应，不是生产账户数据；生产 Host、Vercel、DNS 与真实跨域链路仍为 `not-tested`，必须在获得部署授权后验证。
- 2026-09-20 用户指定云端 Host 为 `https://pair-demo.xiexin.dev`；本地环境和示例变量已更新，Vercel CSP 已收紧到该 Origin。生产 Vercel 环境变量尚未写入。
- 当前权益图表主包约 711 kB（gzip 约 237 kB），构建给出单 chunk 大小提醒但不影响运行。后续可把 ECharts 拆成延迟加载 chunk，避免阻塞首次交付。
- 官网入口未在本次加入；两个应用保持独立，后续可作为单独低风险改动处理。

## 2026-09-20 浏览器反馈修订（PAB-30）

按用户反馈移除背景与盈亏区渐变、删除顶部“免登录 · 只读”标签，金额改为系统无衬线字体与齐线等宽数字；观察室按钮和日期/月选择器统一为 8px 小圆角。该长期约定已同步到 DESIGN.md。

| 用户动作 | 既有入口与状态所有者 | 缺口与决策 | 证据 |
|---|---|---|---|
| 识别交易日 | brief_engine 的新浪交易日历缓存 | Extend：提供只读缓存访问器，公开 calendar 在独立 days 字段投影状态 | 不调用带工作日降级的 `_is_trading_day`；公开请求不拉取日历、不写账户数据 |
| 查看盈亏与操作按钮 | 观察室 CSS Modules、共享 Button 和日期选择器 | Extend：页面内小圆角覆盖，修正日期选择器不存在的圆角 token；维持原交互 | 浏览器 computed style 确认为 8px；金额为系统字体、lining-nums tabular-nums |

交易状态为确定性日历事实：已有日历范围内区分交易日与休市，周末休市，未覆盖的工作日明确为待确认，不涉及模型推断。账户快照存在与否不决定交易状态；窄屏金额使用千/万/亿缩写，完整值保留在 title 与无障碍标签。未改变原加载、刷新、空快照、错误和权限状态；新增日历未知状态。确认/撤销不适用，只读展示。

验收环境仍为 3198 production build + 3199 只读演示 API；本轮结论为本地反馈修订可交付，生产真实日历加载链路仍未验证。

| 关注项 | 状态 | 证据 |
|---|---|---|
| 视觉反馈 | passed | 854px 原视口已刷新；页面 DOM 中渐变背景数量为 0，顶部标签移除，主要按钮 8px 圆角，金额字体已替换 |
| 明暗与响应式 | passed | 检查 1440、1024、768、390px 真实截图；390px 深浅主题日历均可辨认交易状态，紧凑金额不再溢出格子 |
| 日期与月份交互 | passed | 日期弹层按钮圆角 8px，Escape 返回页面；切换未覆盖月份显示待确认提示；恢复九月后交易日与休市正确显示 |
| 控制台 | passed | 本轮交互后的 warning/error 日志为空 |
| 真实账户及生产日历 | not-tested | 演示环境使用测试响应，正式环境需已有后台日历缓存覆盖目标日期，否则工作日显示待确认 |

支持证据：公开观察室及既有交易日历共 17 项后端测试通过，覆盖假日、周末、覆盖范围外日期、空来源、无快照和只读无拉取；严格 TypeScript 检查与生产构建通过。纯视觉改动以真实浏览器检查为主，未运行仓库级 GUI/Web 全量套件。演示九月假期参照[上交所 2026 年休市安排](https://www.sse.com.cn/disclosure/dealinstruc/closed/c/c_20251222_10802510.shtml)，不会写入正式账户数据。

## 2026-09-20 指定云端地址连通性检查

按用户要求将 `VITE_PUBLIC_API_BASE_URL` 改为 `https://pair-demo.xiexin.dev`，移除临时构建脚本对本地演示 API 的强制覆盖，重新构建并刷新 3198 页面。该页面现在实际请求云端，不再展示本地演示账户。

对 `/api/public/performance/v1/overview?date=2026-09-20` 的 GET 实测：本地 Origin `http://127.0.0.1:3198` 返回 403 `forbidden`，正式观察室 Origin `https://pair-observe.xiexin.dev` 同样返回 403；不带 Origin 返回 401 `{"code":"auth-required"}`。响应没有允许跨域读取的标头。页面正确显示读取失败，真实数据链路判定为 failed；返回值说明该路径仍被登录保护拦截，无法据此判断实际数据是否存在。后续需部署并启用云端公开路由，按部署来源配置 Origin，再验证账户快照。未修改云端鉴权、环境变量或服务。

## 2026-09-20 安全加固复核（PAB-30）

用户授权本地优化后，按既有能力审计与真实产品验收流程收紧公开许可、内部写端、后端生命周期及资源限额；未提交、推送或部署。发布许可使用部署端精确内容 ID 白名单，不引入第二套账户账本，不把旧成交或自动生成记录视为已获公开许可；代价是首版必须逐份批准，新快照不会自动展示。配置与限额的唯一说明见 [Host 网关](../../frontend/packages/host/public-observatory/README.md)。

回归先证明原实现存在默认公开、未鉴权写入、内部错误透传、重复参数、错误标头丢失和 Loader 导出问题，再修复。独立只读审查补充发现极端金额精度及下行慢连接缺口，两项已修复并新增回归；最终复审无新增阻断项。Runtime 的 7 项旧 credential/redaction 测试依赖本机虚拟环境，A/B 对比确认基线同样失败；仅为测试注入独立路径后 61 项原样通过，未改变生产路径解析。

支持证据：Python 35 项通过（账户投影、存储、交易日历），TypeScript 97 项通过（网关、Runtime、信任、启动、浏览器 API、真实 Loader/HTTP/Python），既有 WebServer/WebAuth 19 项通过，共 151 项。严格类型检查覆盖当前公开前端、网关及 Runtime 源码；独立前端 production build 与网关/不变量入口的 ESM 构建加载通过，`git diff --check` 通过。受限 worktree 复用本机已安装依赖与临时测试解析配置；未运行仓库级全量 CI、完整发布 bundle、平台矩阵或生产部署。

本轮 UAT 使用 production build（3198）、实际 Host 网关（3199）、真实 Python 公开路由与自动清理的临时合成账户；不启动交易业务 lifespan，不读写真实账户。画面中的“合成测试样本”、¥102,480.00 总权益和 1 条系统活动均来自测试夹具。截图在本轮浏览器工具结果中内联留存，未生成仓库图片文件。

| 关注项 | 状态 | 可观察证据与限制 |
|---|---|---|
| 免登录真实读取链路 | passed | 浏览器 → Host → Python → 临时 holdings 文档，概览/持仓/曲线/日历/活动正常；自动化另检查六路由读取前后文档字节完全一致 |
| 公开范围与脱敏 | passed | 只显示批准快照的 1 条固定系统摘要；同文档私有成交不出现；异常原因中的测试私有路径不出现，只显示固定陈旧提示 |
| 撤销与恢复 | passed | 测试进程撤销批准后刷新，金额、持仓和活动清空并显示尚未配置；重新批准后刷新恢复；旧 ETag 不能返回撤销内容 |
| 主题与窄屏 | passed | 390px 深色与 1440px 浅色检查、活动详情展开及返回；样式没有新增，本轮仅重验安全相关状态，其余视口复用前次未变样式证据 |
| 私有接口及资源保护 | passed | 既有登录测试通过，未认证内部快照 POST 为 403；真实 HTTP 对超额请求返回 429，暂停读取 6 MiB 响应的慢客户端在截止时间后断开并释放并发额度 |
| 生产账户、部署与跨域 | not-tested | 未部署修复；验收后已关闭临时 3199/Python 服务并恢复 `https://pair-demo.xiexin.dev` 构建配置，浏览器仍显示生产公开读取失败 |

结论：本轮本地安全加固可交付；线上可用性仍未验收，不能据本地结果宣称生产已安全上线。上线前由部署负责人核对后端端口隔离、准确 Origin、批准 ID 与私有写 token、多实例入口限流；账户快照的备份恢复和自动生产/审批入口作为后续能力，不用本轮测试替代其验证。

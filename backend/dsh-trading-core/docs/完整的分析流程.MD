
### 1. 端到端 /analyze 任务（贵州茅台 600519）

**SSE 进度流事件（全部正常推送，无 102 错误）**：
- `📊 市场分析师` → `💬 社交媒体分析师`（×2 轮工具调用）→ `📰 新闻分析师` → `💼 基本面分析师`（×2）→ `🐂 看涨研究员` → `🐻 看跌研究员` → `🤝 多空辩论` → `🛡 风险三方讨论` → `📈 交易员生成决策`
- 全程 15 次 ping 心跳，链路稳定

**最终状态**：`status=done`, `error=null` ✅）

**最终 Signal（真实 DeepSeek 推理结果）**：
| 字段 | 值 |
|---|---|
| `signal_type` | `final` |
| `ticker` / `company_name` | `600519` / `贵州茅台` |
| `action` | **卖出** |
| `target_price` | 1350.0 |
| `confidence` | 0.7 |
| `risk_score` | 0.7 |
| `reasoning` | 基本面下行趋势未止（批价下跌、预收款减少），估值中枢可能系统性下移，资金面承压且催化剂缺失，当前风险收益比不佳，建议卖出或回避，等待更明确的见底信号。 |
| `model_info` | `NormalizedChatOpenAI:deepseek-chat` |
| `risk_profile` | `balanced` |
| `calibration` | `false`（无画像护栏强覆盖修正）|

**reports 产出** 5 份中文 Markdown 报告：`market`（技术分析）、`fundamentals`（基本面）、`news`（新闻）、`sentiment`（情绪）、`debate`（多空辩论+投资计划）、`trader`（最终交易决策）。

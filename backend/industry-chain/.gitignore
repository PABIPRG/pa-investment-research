# industry-chain 本地环境
env/
__pycache__/
*.pyc
logs/

# dsh-plugin npm 依赖
dsh-plugin/node_modules/

# 种子数据（可刷新，不进 git）：运行 init.sh 或 scripts/fetch_seed_data.py 下载
data/seed/

# 环境变量（含密钥，勿提交）
.env

# 研报管线产物（可重建，不进 git）
data/reports/
data/a_share_universe.json

# LLM 补边产物（可重建，不进 git）：scripts/build_llm_links.py 生成
data/a_share_llm_links.json

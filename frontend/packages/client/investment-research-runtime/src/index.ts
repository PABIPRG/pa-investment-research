/** Host loader entry for the investment Runtime Client facade exported from `./client`. */

/** Host plugin body — no host-side behavior for the investment Runtime Client facade. */
export function apply(): void {}
